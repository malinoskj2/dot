[user]
	name = Jesse Malinosky
	email = jesse@malinoskj2.dev
	signingkey = D3A582B1949A39501D3913E387E5ECE8E0493976

[alias]
	pom = push origin master
	cloners = clone --recurse-submodules
	ruop = remote update origin --prune
	lg = log --color --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit

[color]
	ui = auto

[apply]
	# Detect whitespace errors when applying a patch
	whitespace = fix

[core]
	editor = vim
	pager = more
	autocrlf = false
	excludesfile = /home/jesse/env/config/git/gitignore_global

[merge]
	ff = yes

[commit]
	gpgsign = true

[gpg]
	program = gpg2

[credential]
	helper = cache --timeout 43200

[diff]
	# Detect copies as well as renames
	renames = copies
