path=(
	~/.cargo/bin
	~/.npm/global/bin
	~/.local/bin
	/sbin
	/bin
	/usr/sbin
	/usr/bin
	/usr/local/sbin
	/usr/local/bin:$HOME/bin
	~/env/script/*/
	/disk/work/projects/rust/yamlink/target/debug
)

if [ "$(hostname)" = 'katana' ] || 
   [ "$(hostname)" = 'home2' ];then
  path=(
  	~/resource/app/linux
	$path
  )
fi

fpath=(
	~/env/zsh/mod/* 
	$fpath
)

